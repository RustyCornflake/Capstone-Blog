<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Racecar Life Blog</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap" rel="stylesheet">
</head>
<body class="bg-dark text-light">
  <header class="bg-black text-white py-5 text-center">
    <div class="container">
      <h1 class="display-4 fw-bold">Race Car Life 🏁</h1>
      <p class="lead">Where Burnouts Become Stories</p>
      <nav class="mt-3">
        <a href="/" class="btn btn-outline-warning me-2">Home</a>
        <a href="/new" class="btn btn-warning">New Post</a>
      </nav>
    </div>
  </header>

  <main class="container my-5">
    <% if (posts.length === 0) { %>
      <p class="text-center">No burnout stories yet. Start one <a href="/new" class="text-warning">here</a>!</p>
    <% } else { %>
      <div class="row g-4">
        <% posts.forEach(post => { %>
          <div class="col-md-6 col-lg-4">
            <div class="card bg-secondary text-light h-100 shadow">
              <div class="card-body d-flex flex-column">
                <h5 class="card-title text-warning"><%= post.title %></h5>
                <p class="card-text"><%= post.content %></p>
                <div class="mt-auto d-flex justify-content-between">
                  <a href="/edit/<%= post.id %>" class="btn btn-outline-light btn-sm">Edit</a>
                  <form action="/delete/<%= post.id %>" method="POST">
                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </main>

  <footer class="bg-black text-center py-4">
    <div class="container">
      <p class="mb-0 text-secondary">&copy; 2025 RaceCarLife. All Rights Reserved.</p>
    </div>
  </footer>
</body>
</html>